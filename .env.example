# Clerk publishable key (starts with pk_test_ or pk_live_).
# Get it from https://dashboard.clerk.com → API Keys. Copy this file to `.env` and fill it in.
# Enable Google and GitHub as social connections in the Clerk dashboard for OAuth to work.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
